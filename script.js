const header=document.getElementById("siteHeader");const menuToggle=document.getElementById("menuToggle");const mainNav=document.getElementById("mainNav");const navLinks=[...document.querySelectorAll(".main-nav a")];const sections=[...document.querySelectorAll("main section[id]")];const revealItems=document.querySelectorAll(".reveal");function setMenu(open){menuToggle.classList.toggle("active",open);mainNav.classList.toggle("open",open);menuToggle.setAttribute("aria-expanded",String(open));menuToggle.setAttribute("aria-label",open?"메뉴 닫기":"메뉴 열기");document.body.classList.toggle("menu-open",open)}menuToggle.addEventListener("click",()=>setMenu(!mainNav.classList.contains("open")));navLinks.forEach(link=>link.addEventListener("click",()=>setMenu(false)));window.addEventListener("scroll",()=>{header.classList.toggle("scrolled",window.scrollY>30);let current="home";sections.forEach(section=>{if(window.scrollY>=section.offsetTop-180)current=section.id});navLinks.forEach(link=>link.classList.toggle("active",link.getAttribute("href")===`#${current}`))},{passive:true});const revealObserver=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("visible");revealObserver.unobserve(entry.target)}})},{threshold:.14});revealItems.forEach(item=>{if(!item.classList.contains("visible"))revealObserver.observe(item)});const canvas=document.getElementById("starCanvas");const ctx=canvas.getContext("2d");let stars=[];let animationId;function resizeCanvas(){const ratio=Math.min(window.devicePixelRatio||1,2);canvas.width=window.innerWidth*ratio;canvas.height=window.innerHeight*ratio;canvas.style.width=`${window.innerWidth}px`;canvas.style.height=`${window.innerHeight}px`;ctx.setTransform(ratio,0,0,ratio,0,0);const count=Math.min(95,Math.floor(window.innerWidth/11));stars=Array.from({length:count},()=>({x:Math.random()*window.innerWidth,y:Math.random()*window.innerHeight,r:Math.random()*1.1+.2,a:Math.random()*.5+.08,s:Math.random()*.08+.015}))}function drawStars(){ctx.clearRect(0,0,window.innerWidth,window.innerHeight);stars.forEach(star=>{star.y+=star.s;if(star.y>window.innerHeight+4){star.y=-4;star.x=Math.random()*window.innerWidth}ctx.beginPath();ctx.arc(star.x,star.y,star.r,0,Math.PI*2);ctx.fillStyle=`rgba(238,218,159,${star.a})`;ctx.fill()});animationId=requestAnimationFrame(drawStars)}const reducedMotion=window.matchMedia("(prefers-reduced-motion: reduce)");function updateAnimation(){cancelAnimationFrame(animationId);resizeCanvas();if(!reducedMotion.matches)drawStars();else ctx.clearRect(0,0,window.innerWidth,window.innerHeight)}window.addEventListener("resize",updateAnimation);reducedMotion.addEventListener?.("change",updateAnimation);updateAnimation();
